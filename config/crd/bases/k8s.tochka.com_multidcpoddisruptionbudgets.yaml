---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: multidcpoddisruptionbudgets.k8s.tochka.com
spec:
  group: k8s.tochka.com
  names:
    kind: MultidcPodDisruptionBudget
    listKind: MultidcPodDisruptionBudgetList
    plural: multidcpoddisruptionbudgets
    singular: multidcpoddisruptionbudget
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: MultidcPodDisruptionBudget is the Schema for the multidcpoddisruptionbudgets
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MultidcPodDisruptionBudgetSpec defines the desired state
              of MultidcPodDisruptionBudget
            properties:
              maxUnavailable:
                description: which is a description of the number of pods from that
                  set that can be unavailable after the eviction. It can be either
                  an absolute number or a percentage.
                type: string
              minAvailable:
                description: |-
                  An eviction is allowed if at least "minAvailable" pods selected by
                  "selector" will still be available after the eviction, i.e. even in the
                  absence of the evicted pod.
                type: string
              selector:
                additionalProperties:
                  type: string
                type: object
            required:
            - selector
            type: object
          status:
            description: MultidcPodDisruptionBudgetStatus defines the observed state
              of MultidcPodDisruptionBudget
            type: object
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
      status: {}
